import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#34640d7f",
      dark: "#1a4821",
      contrastText: "#fff",
    },
    secondary: {
      main: "rgb(177, 65, 65, 0.8)",
      contrastText: "#000",
      dark: "#b14141",
    },
  },
});
